#!/bin/bash
set -e

export HOME=/root
export TERM=xterm
NC='\e[0m'
dateFromServer=$(curl -v --insecure --silent https://google.com/ 2>&1 | grep Date | sed -e 's/< Date: //')
biji=`date +"%Y-%m-%d" -d "$dateFromServer"`
ipsaya=$(curl -sS ipv4.icanhazip.com)

Repo1="https://raw.githubusercontent.com/darnix1/permission/main/"
export MYIP=$(curl -sS ipv4.icanhazip.com)
SELLER=$(curl -sS ${Repo1}ip | grep $MYIP | awk '{print $2}')
Exp100=$(curl -sS ${Repo1}ip | grep $MYIP | awk '{print $3}')
data_ip="https://raw.githubusercontent.com/darnix1/permission/main/ipmini"
d2=$(date -d "$date_list" +"+%s")
d1=$(date -d "$Exp" +"+%s")
dayleft=$(( ($d1 - $d2) / 86400 ))

TOKEN="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"
REPO="https://github.com/darnix1/permission.git"
EMAIL="fdanx@yahoo.com"
USER="Tomketstore"
today=$(date -d "0 days" +"%Y-%m-%d")

# Clonar el repositorio
git clone ${REPO} /root/ipvps/

echo -e ""
echo -e ""
read -p "Input IP Address : " ip
CLIENT_EXISTS=$(grep -w $ip /root/ipvps/ip | wc -l)
if [[ ${CLIENT_EXISTS} == '1' ]]; then
  echo "IP Already Exist !"
  rm -rf /root/ipvps
  exit 0
fi

echo -e ""
read -p " Input usu : " name
echo -e ""
clear
read -p " Masukan waktu expired : " exp
exp2=$(date -d "${exp} days" +"%Y-%m-%d")
echo "### ${name} ${exp2} ${ip}" >> /root/ipvps/ip

cd /root/ipvps
git config --global user.email "${EMAIL}"
git config --global user.name "${USER}"

# Eliminar cualquier configuración de git anterior para iniciar limpio
rm -rf .git &> /dev/null
git init &> /dev/null
git add . &> /dev/null
git commit -m "Adding IP ${ip} with user ${name} expiring on ${exp2}" &> /dev/null
git branch -M main &> /dev/null
git remote add origin ${REPO}

# Mostrar detalles de los cambios antes del push
git status
git log --oneline

# Hacer el push y capturar cualquier error
if git push -f https://${TOKEN}@github.com/darnix1/permission.git 2>&1; then
  echo -e "Push to GitHub successful."
else
  echo -e "Failed to push to GitHub."
fi

rm -rf /root/ipvps

echo -e "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo -e "🍀VPS SUCCESSFULLY REGISTERED🍀"
echo -e "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo -e "🌹NAME AUTHOR : $name"
echo -e "🏵️SCRIPT TIME : $exp2"
echo -e "🌺IP SERVER   : $ip"
echo -e "wget --no-check-certificate https://scpremium.kiritotunneling.my.id:81/nana_x_aurora.sh && chmod +x nana_x_aurora.sh && ./nana_x_aurora.sh"
echo -e "━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
